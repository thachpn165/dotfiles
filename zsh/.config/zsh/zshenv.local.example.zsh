# Machine-specific environment that must load before .zprofile/.zshrc belongs here.
# This file lives outside the repo at ~/.config/zshenv.local.zsh.

# [ -f "$HOME/.cargo/env" ] && source "$HOME/.cargo/env"
